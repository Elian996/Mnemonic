import { LoadingLine } from "@/components/loading-line";

export default function Loading() {
  return (
    <main className="mn-site-loading">
      <LoadingLine label="正在加载页面" />
    </main>
  );
}
